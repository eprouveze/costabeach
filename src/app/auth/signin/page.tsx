import React from 'react';
import LoginForm from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Sign In | Costa Beach',
  description: 'Sign in to your Costa Beach account',
};

export default function SignInPage() {
  return <LoginForm />;
}
