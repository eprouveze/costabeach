import React from 'react';

export const metadata = {
  title: 'Sign Up | Costa Beach',
  description: 'Create a Costa Beach account',
};

export default function SignUpLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
} 