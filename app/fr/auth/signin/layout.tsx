import React from 'react';

export const metadata = {
  title: 'Se connecter | Costa Beach',
  description: 'Connectez-vous à votre compte Costa Beach',
};

export default function SignInLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
} 