import React from 'react';

export const metadata = {
  title: "S'inscrire | Costa Beach",
  description: 'Créez un compte Costa Beach',
};

export default function SignUpLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
} 